---
# SECRETS - Copy this to vault.yml and fill in your values
# vault.yml is gitignored and should never be committed
#
# For extra security, encrypt with ansible-vault:
#   ansible-vault encrypt group_vars/vault.yml
#
# Then run playbooks with --ask-vault-pass

# Database
vault_db_password: "your-database-password"

# Django secret key (generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
vault_secret_key: "your-secret-key-here"

# Stripe keys (from dashboard.stripe.com)
vault_stripe_public_key: "pk_live_..."
vault_stripe_secret_key: "sk_live_..."
vault_stripe_webhook_secret: "whsec_..."  # Main webhook for payments
vault_stripe_connect_webhook_secret: "whsec_..."  # Connect webhook for seller accounts

# Stripe subscription price IDs (create in Stripe Dashboard > Products)
vault_stripe_price_basic: "price_..."  # $9.99/month Basic tier
vault_stripe_price_featured: "price_..."  # $29.99/month Featured tier
vault_stripe_price_premium: "price_..."  # $99.99/month Premium tier

# DigitalOcean Spaces (for media storage)
vault_do_spaces_key: ""
vault_do_spaces_secret: ""

# Google OAuth (from console.cloud.google.com > APIs & Services > Credentials)
vault_google_client_id: ""
vault_google_client_secret: ""

# Email password (if using authenticated SMTP)
vault_email_host_password: ""

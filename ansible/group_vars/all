---
# Server configuration
server_user: www
app_name: heroesandmore
domain: heroesandmore.com

# Git repository
git_repo: https://github.com/nadermx/heroesandmore.git
git_branch: main

# Paths
app_path: /home/{{ server_user }}/{{ app_name }}
venv_path: "{{ app_path }}/venv"
config_path: "{{ app_path }}/config.py"

# Database (password from vault.yml)
db_name: heroesandmore
db_user: heroesandmore
db_password: "{{ vault_db_password }}"

# Python version
python_version: "3.12"

# Supervisor
supervisor_conf: /etc/supervisor/conf.d/{{ app_name }}.conf

# Nginx
nginx_conf: /etc/nginx/sites-available/{{ app_name }}

# Celery
celery_workers: 2

# Redis
redis_url: redis://localhost:6379/0

# Config template variables (from vault.yml for secrets)
django_secret_key: "{{ vault_secret_key | default('change-me') }}"
stripe_public_key: "{{ vault_stripe_public_key | default('') }}"
stripe_secret_key: "{{ vault_stripe_secret_key | default('') }}"
stripe_webhook_secret: "{{ vault_stripe_webhook_secret | default('') }}"
stripe_connect_webhook_secret: "{{ vault_stripe_connect_webhook_secret | default('') }}"
stripe_price_basic: "{{ vault_stripe_price_basic | default('') }}"
stripe_price_featured: "{{ vault_stripe_price_featured | default('') }}"
stripe_price_premium: "{{ vault_stripe_price_premium | default('') }}"
do_spaces_key: "{{ vault_do_spaces_key | default('') }}"
do_spaces_secret: "{{ vault_do_spaces_secret | default('') }}"
